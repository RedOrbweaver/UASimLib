#pragma once
#include "UASimLib.hpp"

constexpr float R0_ATTEN = 0.0001f; // [m] � near-field cap (np. 5 cm)
constexpr float SOUND_V = 1440.0f / 1.0f; // C++

inline int ktore_odbicie = 0;

constexpr float DEFAULT_INITIAL_WAVE_RADIUS = 0.02f;